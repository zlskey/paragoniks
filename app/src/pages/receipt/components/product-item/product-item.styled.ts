import { AvatarGroup, Paper, styled } from '@mui/material'

export const ProductItemAvatarGroup = styled(AvatarGroup)`
  transform: scale(0.55) translate(50%, 50%);
  position: absolute;
  bottom: 0;
  right: 0;
`

export const ProductItemContainer = styled(Paper)`
  position: relative;
`
